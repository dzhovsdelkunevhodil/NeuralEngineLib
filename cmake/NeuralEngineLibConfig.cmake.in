@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")

set(NEURAL_ENGINE_USE_GPU @NEURAL_ENGINE_USE_GPU@)
set(NEURAL_ENGINE_ENABLE_LOGGING @NEURAL_ENGINE_ENABLE_LOGGING@)

if(NEURAL_ENGINE_USE_GPU)
    set_target_properties(Backend PROPERTIES
        INTERFACE_COMPILE_DEFINITIONS NEURAL_ENGINE_USE_GPU=1)
else()
    set_target_properties(Backend PROPERTIES
        INTERFACE_COMPILE_DEFINITIONS NEURAL_ENGINE_USE_GPU=0)
endif()

if(NEURAL_ENGINE_ENABLE_LOGGING)
    set_target_properties(Utils PROPERTIES
        INTERFACE_COMPILE_DEFINITIONS NEURAL_ENGINE_LOGGING=1)
endif()